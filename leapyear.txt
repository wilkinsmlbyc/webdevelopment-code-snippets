leapyear


function isLeap(year) {
    
/**************Don't change the code above****************/    
    
    //Write your code here.    year = prompt("Please input year");
    var output;
    
    if ((year % 4 === 0 && year % 100 !== 0) || (year % 400 === 0) ) {
        output = "Leap year.";
    }
    else {
        output = "Not leap year.";
    }

    return output;

    

/**************Don't change the code below****************/    

}